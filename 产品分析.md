# Astro 零门槛博客桌面端
## 产品分析与迭代计划文档（完整版·2025-08-12）

---

## 1. 文档目的

为团队（含 UI、前端、Rust、测试）提供一份可直接开工的「做什么、怎么做、何时做完」的单一事实源。

---

## 2. 产品愿景

让任何人在 3 分钟内把「任何来源的旧文章 + 本地图片」变成可发布的 Astro 博客，并通过可视化界面完成 Git 版本管理、写作、部署全流程，无需命令行。

---

## 3. 目标用户画像

### Persona-A：新手内容创作者
- 年龄：20-35 岁
- 特点：完全不会命令行
- 痛点：想拥有独立域名博客，但 Hexo/Hugo 步骤多、部署难

### Persona-B：有内容迁移需求的用户
- 特点：已有 Hexo/Hugo/Notion 输出，文章 50-500 篇
- 痛点：迁移工作量大、图片路径易错乱

---

## 4. 需求痛点拆解

| 痛点 | 当前方案 | 产品解法 | 可量化指标 |
|------|----------|----------|------------|
| 写作-发布链路长 | CLI + 手动 push | 一键新建→编辑→部署 | 首次上线耗时 ≤3 min |
| 本地图片混乱 | 手动复制到 public | 拖拽自动 hash 化托管 | 迁移后图片 404 率 ≤1 % |
| 旧内容迁移难 | 复制粘贴 + 改路径 | 批量导入 + 自动改写引用 | 100 篇文章迁移 ≤10 min |

---

## 5. 功能全景（MoSCoW + 技术落地）

| 编号 | 功能 | 优先级 | 技术实现 | 验收标准 |
|------|------|--------|----------|----------|
| F1 | 新建 Astro 站点 | Must | 内置模板 git clone | 30 秒内出现可 dev 的目录 |
| F2 | Markdown 所见即所得 | Must | Monaco Editor + markdown-it | 支持代码高亮、数学公式 |
| F3 | 本地实时预览 | Must | Tauri shell `astro dev` | 保存后 2 秒内热刷新 |
| F4 | 一键部署 GH Pages | Must | GitHub REST API + force-push gh-pages | 30 秒内给出公开 URL |
| F5 | 本地图片自动托管 | Must | Rust 计算 SHA-256 → /public/assets/images | 原图零丢失，引用全部可访问 |
| F6 | 批量导入旧文章 | Must | gray-matter 解析 + 路径改写 | 100 篇文章 <10 min |
| F7 | 分类/标签管理 | Must | 只读 front-matter category & tags → 树形 UI | 分类层级 ≤3 级 |
| F8 | Git 全流程 | Should | Rust git2 + Octokit | clone/commit/push/PR 零命令行 |
| F9 | 动态表单 | Should | zod-to-json-schema + uniforms | 与 Astro Content Schema 100% 对齐 |
| F10 | 多平台部署 | Could | Vercel/Netlify REST API | 配置一次即可 |
| F11 | 主题商店 | Could | 远程主题 JSON 索引 | 一键切换 |
| F12 | 图片资源管理 | Should | Rust 文件扫描 + 引用追踪 | 可展示、可定位、安全删除 |

---

## 6. 系统架构

```
┌────────────────────────────┐
│ Tauri (Rust)               │
│ • git2 / ssh-agent         │
│ • fs watcher & imagemin    │
│ • file-dialog & shell      │
└──────────┬─────────────────┘
           │ invoke/handle
┌──────────┴─────────────────┐
│ React + TypeScript         │
│ • Monaco Editor            │
│ • uniforms 表单            │
│ • React-Router + Zustand   │
└────────────────────────────┘
```

**数据流**：
Markdown → front-matter → 分类标签 Store → UI  
图片 → SHA-256 → /public/assets → URL 映射表 → Markdown 引用改写

---

## 7. 迭代计划（14 天两阶段）

| 阶段 | 时间 | 目标 | 关键里程碑 | 验收指标 |
|------|------|------|------------|----------|
| MVP-1 | Day 0-6 | 最小闭环：新建→编辑→预览→部署 | • Day0 环境 & 模板<br>• Day3 本地预览<br>• Day5 一键部署<br>• Day6 5 位小白内测 | 首次上线 ≤3 min，成功率 ≥80 % |
| MVP-2 | Day 7-13 | 旧内容与图片迁移 | • Day7-8 批量导入<br>• Day9 图片 hash 化<br>• Day11 分类标签树<br>• Day13 10 位老用户迁移 | 迁移成功率 ≥90 %，7 天留存 ≥60 % |

每日任务已排入甘特图（见附件）。

---

## 8. 关键 KPI

| KPI | 定义 | 目标值 |
|-----|------|--------|
| TTFHW (Time To First Hello World) | 安装到线上可访问 | ≤3 min |
| Migration Success Rate | 旧文章+图片无 404 | ≥90 % |
| 7-Day Retention | 内测用户 7 天后仍在使用 | ≥60 % |
| CLI Zero | 用户无需手动输入命令 | 100 % |

---

## 9. 风险与对策

| 风险 | 概率 | 对策 |
|------|------|------|
| 不同 front-matter 格式 | 中 | 提供映射表 + 手动修正面板 |
| 大仓库克隆耗时 | 低 | 进度条 + 后台异步 |
| 启动时间过长 | 中 | 懒加载 + 缓存机制 + 后台任务 |

---

## 10. 启动性能优化策略

考虑到应用启动时需要处理多项任务，为确保良好的用户体验，我们将采用以下优化策略：

### 10.1 懒加载机制
- 仅加载核心功能（如新建站点、Markdown编辑器）
- 其他功能（如图片资源管理、分类标签）在用户首次访问时加载

### 10.2 缓存机制
- 对图片引用关系等计算结果进行缓存
- 使用增量更新策略，仅在文件发生变化时重新计算

### 10.3 后台任务处理
- 将耗时操作（如全量图片扫描）放到后台执行
- 提供进度指示器让用户了解处理状态

### 10.4 异步初始化
- 采用异步方式初始化各项服务
- 允许用户在部分功能初始化完成前就开始使用核心功能

---

## 11. 功能清单与实现方案（面向开发者）

### 11.1 功能总览

| 编号 | 功能名称 | 一句话描述 |
|------|----------|------------|
| F1 | 新建 Astro 站点 | 一键生成可运行的 Astro 博客模板 |
| F2 | Markdown 编辑器 | WYSIWYG + 快捷键 + 代码高亮 |
| F3 | 本地实时预览 | 保存即热重载 |
| F4 | 一键部署 | GitHub Pages / Vercel / Netlify 三平台可选 |
| F5 | 本地图片自动托管 | 识别本地图片→hash→public，引用自动改写 |
| F6 | 批量导入旧文章 | 拖拽/选文件夹或 GitHub 仓库，解析 front-matter |
| F7 | 分类 & 标签管理 | 纯读取 front-matter 的 `category` & `tags` |
| F8 | Git 全流程 | clone / commit / push / PR，SSH 或 Token 登录 |
| F9 | 动态表单 | 读取 Astro Content Schema → uniforms 自动渲染 |
| F10 | 博客配置 | 站点标题、描述、社交链接可视化修改 |
| F11 | 图片资源管理 | 展示引用关系，安全删除未使用图片 |

### 11.2 推荐实现方案（技术选型 + 关键 API）

| 功能 | 技术选型 & 关键依赖 | 核心实现路径 |
|------|---------------------|--------------|
| **F1 新建站点** | 内置模板 Git 仓库 (`astro-minimal-blog-template`) | Rust `git2::Repository::clone(url, path)` |
| **F2 Markdown 编辑器** | Monaco Editor + markdown-it + front-matter 解析 | React 端：`@monaco-editor/react`；保存时调用 `gray-matter` 解析 |
| **F3 本地预览** | `astro dev` | Tauri `Command::new("astro").args(["dev"])`，端口固定 4321 |
| **F4 一键部署** | • GitHub Pages：force-push `gh-pages` 分支<br>• Vercel/Netlify：REST API + CLI | 统一封装为 Rust command `deploy(platform, token)` |
| **F5 图片托管** | Imagemin + SHA-256 | Rust `sha2::Sha256::digest()` → 复制到 `/public/assets/images/{hash[..2]}/{hash}.ext`，前端正则替换 `![alt](path)` |
| **F6 批量导入** | • 本地：递归 glob `.md`<br>• GitHub：Octokit REST + `git2::Repository::clone` | 解析 `gray_matter::extract` → 写入 `src/content/blog` |
| **F7 分类标签** | 纯前端树形组件 + 虚拟列表 | React `react-arborist`；数据来自 `front-matter` |
| **F8 Git 全流程** | • Auth：SSH key 或 GitHub PAT<br>• 操作：git2 | Rust commands：<br>`git_clone`, `git_commit`, `git_push`, `git_pr` |
| **F9 动态表单** | • Astro Schema → JSON Schema<br>• uniforms 渲染 | `zod-to-json-schema` → `@vazco/uniforms-unstyled` |
| **F10 博客配置** | 修改 `astro.config.mjs` & `_config.yml` | 前端表单 → Rust 写回文件，保留注释 |
| **F11 图片资源管理** | Rust 文件扫描 + 正则表达式匹配 | • 扫描 `/public/assets/images` 目录建立图片库<br>• 使用正则表达式扫描所有 Markdown 文件追踪引用<br>• 建立图片与文章的双向引用关系<br>• 只有引用计数为0的图片才允许删除 |